<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&family=Space+Mono:wght@400;700&display=swap');
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: white;
      width: 1200px;
      height: 630px;
      overflow: hidden;
    }
    
    .font-mono {
      font-family: 'Space Mono', monospace;
    }
    
    .card {
      width: 1200px;
      height: 630px;
      border: 6px solid #000;
      background: white;
      display: flex;
      flex-direction: column;
    }
    
    .card-header {
      padding: 48px;
      border-bottom: 6px solid #000;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    
    .badge {
      display: inline-block;
      background: #0066FF;
      color: white;
      padding: 8px 16px;
      font-family: 'Space Mono', monospace;
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      margin-bottom: 24px;
    }
    
    .title {
      font-size: 56px;
      font-weight: 900;
      line-height: 1.1;
      letter-spacing: -0.02em;
      margin-bottom: 20px;
      max-width: 900px;
    }
    
    .subtitle {
      font-size: 28px;
      color: rgba(0,0,0,0.6);
      font-style: italic;
      max-width: 800px;
    }
    
    .card-footer {
      padding: 32px 48px;
      background: #F5F5F5;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .metrics {
      display: flex;
      gap: 12px;
    }
    
    .metric-tag {
      font-family: 'Space Mono', monospace;
      font-size: 12px;
      padding: 6px 12px;
      border: 1px solid rgba(0,0,0,0.2);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .brand {
      font-family: 'Space Mono', monospace;
      font-size: 24px;
      font-weight: bold;
      color: #000;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="card-header">
      <span class="badge">Fact Check</span>
      <h1 class="title"><%= fact_check.title %></h1>
      <% if fact_check.subtitle %>
        <p class="subtitle">"<%= fact_check.subtitle %>"</p>
      <% end %>
    </div>
    
    <div class="card-footer">
      <div class="metrics">
        <% fact_check.metrics.first(4).each do |metric| %>
          <span class="metric-tag"><%= metric.tr("_", " ").split.first(2).join(" ") %></span>
        <% end %>
        <% if fact_check.metrics.size > 4 %>
          <span class="metric-tag">+<%= fact_check.metrics.size - 4 %> more</span>
        <% end %>
      </div>
      <span class="brand">EUROPEVERSUS</span>
    </div>
  </div>
</body>
</html>
