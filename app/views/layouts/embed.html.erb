<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= content_for(:title) || "EUROPEVERSUS" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body style="margin: 0; padding: 0; background: transparent;">
    <%= yield %>
  </body>
</html>
