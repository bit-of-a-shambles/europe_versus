<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "EUROPEVERSUS — Evidence-Based European Comparisons" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Compare European statistics with the US, India, and China using evidence-based data.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon -->
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png" sizes="32x32">
    <link rel="apple-touch-icon" href="/icon.png" sizes="180x180">
    <meta name="theme-color" content="#000000">

    <!-- Swiss Brutalist Typography -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      :root {
        --color-black: #000000;
        --color-white: #FFFFFF;
        --color-electric: #0066FF;
        --color-signal: #FFCC00;
        --color-surface: #F5F5F5;
      }
      body {
        font-family: 'Inter', 'Helvetica Neue', Helvetica, Arial, sans-serif;
      }
      .font-mono {
        font-family: 'Space Mono', monospace !important;
      }
      .tracking-brutal {
        letter-spacing: 0.12em;
      }
    </style>
  </head>

  <body class="min-h-screen bg-white text-black">
    <!-- Header -->
    <header class="bg-black text-white">
      <div class="max-w-7xl mx-auto">
        <div class="flex items-center justify-between px-6 py-4">
          <%= link_to root_path, class: "font-mono font-bold text-xl tracking-tight hover:text-[#0066FF] transition-colors" do %>
            EUROPEVERSUS
          <% end %>
          
          <nav class="hidden md:flex items-center gap-8">
            <%= link_to "HOME", root_path, class: "text-xs font-medium tracking-brutal uppercase hover:underline underline-offset-4" %>
            <%= link_to "STATISTICS", statistics_path, class: "text-xs font-medium tracking-brutal uppercase hover:underline underline-offset-4" %>
            <%= link_to "METHODOLOGY", methodology_path, class: "text-xs font-medium tracking-brutal uppercase hover:underline underline-offset-4" %>
            <%= link_to "CONTRIBUTE", root_path(anchor: "contribute"), class: "text-xs font-medium tracking-brutal uppercase bg-[#0066FF] px-4 py-2 hover:bg-white hover:text-black transition-colors" %>
          </nav>
          
          <button class="md:hidden p-2" data-controller="mobile-menu" data-action="click->mobile-menu#toggle">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        
        <!-- Mobile Navigation -->
        <nav class="md:hidden border-t border-white/20 hidden" data-mobile-menu-target="menu">
          <div class="px-6 py-4 space-y-4">
            <%= link_to "HOME", root_path, class: "block text-sm font-medium tracking-brutal uppercase" %>
            <%= link_to "STATISTICS", statistics_path, class: "block text-sm font-medium tracking-brutal uppercase" %>
            <%= link_to "METHODOLOGY", methodology_path, class: "block text-sm font-medium tracking-brutal uppercase" %>
            <%= link_to "CONTRIBUTE", root_path(anchor: "contribute"), class: "block text-sm font-medium tracking-brutal uppercase text-[#0066FF]" %>
          </div>
        </nav>
      </div>
    </header>

    <main>
      <%= yield %>
    </main>
    
    <!-- Footer -->
    <footer class="border-t-2 border-black">
      <div class="max-w-7xl mx-auto px-6 py-8">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6">
          <div>
            <div class="font-mono text-sm font-bold mb-2">EUROPEVERSUS</div>
            <p class="text-sm text-black/60 max-w-md">
              Evidence-based comparisons. Data from Our World in Data, World Bank, OECD, and Eurostat.
            </p>
          </div>
          <div class="flex gap-8 font-mono text-xs uppercase tracking-brutal">
            <%= link_to "Statistics", statistics_path, class: "hover:underline underline-offset-4" %>
            <%= link_to "Methodology", methodology_path, class: "hover:underline underline-offset-4" %>
            <a href="https://github.com/Duartemartins/europe_versus" target="_blank" class="hover:underline underline-offset-4">GitHub</a>
          </div>
        </div>
        <div class="border-t border-black/20 mt-6 pt-6">
          <p class="font-mono text-xs text-black/40">
            © 2025 EUROPEVERSUS · Built by <a href="https://x.com/calltheball_" class="hover:text-[#0066FF] transition-colors">@calltheball_</a>
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>
