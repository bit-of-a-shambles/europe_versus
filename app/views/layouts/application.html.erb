<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "EuropeVersus - Evidence-Based European Comparisons" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Compare European statistics with the US, India, and China using evidence-based data to counter narratives about Europe.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Modern favicon setup -->
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png" sizes="32x32">
    <link rel="apple-touch-icon" href="/icon.png" sizes="180x180">
    <meta name="theme-color" content="#3B82F6">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50">
    <!-- Modern floating header -->
    <header class="sticky top-0 z-50 backdrop-blur-md bg-white/80 border-b border-white/20 shadow-lg">
      <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <%= link_to root_path, class: "flex items-center space-x-3 group" do %>
            <div class="w-10 h-10 bg-gradient-to-r from-blue-600 to-indigo-600 rounded-xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-all duration-300">
              <span class="text-white font-bold text-lg">EU</span>
            </div>
            <div>
              <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent">EuropeVersus</h1>
              <span class="text-xs text-gray-500 hidden sm:block">Evidence-based comparisons</span>
            </div>
          <% end %>
          
          <nav class="hidden md:flex items-center space-x-1">
            <%= link_to "Home", root_path, class: "px-4 py-2 rounded-full text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
            <%= link_to "Statistics", statistics_path, class: "px-4 py-2 rounded-full text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
            <%= link_to "Methodology", methodology_path, class: "px-4 py-2 rounded-full text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
            <%= link_to "Contribute", contribute_path, class: "px-4 py-2 rounded-full bg-gradient-to-r from-green-500 to-emerald-500 text-white hover:from-green-600 hover:to-emerald-600 transition-all duration-200 font-medium shadow-lg hover:shadow-xl" %>
          </nav>
          
          <button class="md:hidden p-2 rounded-xl hover:bg-gray-100 transition-colors" data-controller="mobile-menu" data-action="click->mobile-menu#toggle">
            <svg class="w-6 h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        
        <nav class="md:hidden mt-4 pb-4 border-t border-gray-200 pt-4 hidden" data-mobile-menu-target="menu">
          <%= link_to "Home", root_path, class: "block py-3 px-4 rounded-lg text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
          <%= link_to "Statistics", statistics_path, class: "block py-3 px-4 rounded-lg text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
          <%= link_to "Methodology", methodology_path, class: "block py-3 px-4 rounded-lg text-gray-700 hover:bg-blue-50 hover:text-blue-600 transition-all duration-200 font-medium" %>
          <%= link_to "Contribute", contribute_path, class: "block py-3 px-4 rounded-lg bg-gradient-to-r from-green-500 to-emerald-500 text-white font-medium shadow-lg" %>
        </nav>
      </div>
    </header>

    <main class="container mx-auto px-6 py-12">
      <%= yield %>
    </main>
    
    <!-- Modern footer with gradient -->
    <footer class="relative overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-r from-slate-900 via-blue-900 to-indigo-900"></div>
      <div class="absolute inset-0 bg-[url('data:image/svg+xml,%3Csvg width="60" height="60" viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg"%3E%3Cg fill="none" fill-rule="evenodd"%3E%3Cg fill="%239C92AC" fill-opacity="0.05"%3E%3Ccircle cx="6" cy="6" r="6"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E')]"></div>
      <div class="relative container mx-auto px-6 py-16">
        <div class="text-center">
          <div class="w-16 h-16 bg-gradient-to-r from-blue-500 to-indigo-500 rounded-2xl flex items-center justify-center mx-auto mb-6 shadow-2xl">
            <span class="text-white font-bold text-2xl">EU</span>
          </div>
          <h2 class="text-2xl font-bold text-white mb-4">EuropeVersus</h2>
          <p class="text-blue-100 text-lg mb-6 max-w-2xl mx-auto">Promoting evidence-based understanding of European performance through data-driven insights</p>
          <div class="flex justify-center space-x-6 mb-8">
            <%= link_to "Home", root_path, class: "text-blue-200 hover:text-white transition-colors" %>
            <%= link_to "Statistics", statistics_path, class: "text-blue-200 hover:text-white transition-colors" %>
            <%= link_to "Methodology", methodology_path, class: "text-blue-200 hover:text-white transition-colors" %>
          </div>
          <div class="pt-8 border-t border-blue-800/30">
            <p class="text-blue-300 text-sm">Data sourced from official statistics and reputable international organizations</p>
            <p class="text-blue-400 text-xs mt-2">
              © 2025 EuropeVersus. Built by 
              <a href="https://x.com/calltheball_" target="_blank" rel="noopener noreferrer" class="text-blue-300 hover:text-white transition-colors underline">@calltheball_</a> 
              with Rails 8 & Tailwind CSS
            </p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
